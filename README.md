Experiments with the Boarduino
==============================

git submodule configuration
---------------------------

This repository uses external repositories using git submodules:

    $ git submodule init
    $ git submodule update
